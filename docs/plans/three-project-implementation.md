# Three Project Demonstrations Implementation Plan
Goal: three independent runnable subfolder demos, actual timestamped prompts/responses, verification and lecture integration.
User authorized implementing the three published case designs with subagents. Each worker owns only its project folder; root integrates course and evidence. No commits by workers.
ApplyFlow: reuse existing example transparently; add validated backup import and deletion, preserve storage safety.
FlyBlock: published small game and real connectome-derived neural controller with pinned provenance; reduced actual circuit acceptable if clearly labeled. Fake connections/baseline disguised as brain forbidden.
Signal Magazine: real source and actual AI execution, editorial lifecycle, duplicate prevention and corrections; static recorded run explicitly labeled, secrets only in environment.
Evidence: per-project evidence/events.jsonl with actual UTC stage times and exact public progress messages; real tests.txt; final response.md exactly matching returned handoff. No internal reasoning or secret logging.
Root: test/review/UI verify; gallery and course report with actual elapsed vs sum, stage bars, prompt/response files, demos. Preserve 4x120 minute course; these are agent wall times not student forecasts. Publish only after checks and verify deployed bytes.
