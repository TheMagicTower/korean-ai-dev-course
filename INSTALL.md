# 한국어 개발 스킬 설치 안내

패키지 버전: 0.2.0 · Codex / Claude Code · 프로젝트 단위 설치

이 저장소를 모델에 영구적으로 ‘학습’시키는 과정이 아닙니다. 프로젝트에 `SKILL.md` 파일을 배치해 필요할 때 읽게 합니다. 설치·발견·호출·실제 행동은 각각 확인해야 합니다.

## 설치 전

1. 사용할 프로젝트의 루트 경로와 도구를 확인합니다. 현재 작업 폴더가 학생 프로젝트인지 패키지 다운로드 폴더인지 구분합니다.
2. 공개된 Release 또는 확인한 커밋의 파일을 읽습니다. `main`을 자동으로 최신 업데이트하지 않습니다. 아직 공개 주소·태그가 없는 로컬 묶음을 공개 Release라고 부르지 않습니다.
3. `skills/`의 8개 폴더와 이 안내를 확인합니다. 외부 명령 실행·키 입력·글로벌 설정 변경은 설치에 필요하지 않습니다.

## 복사할 위치

| 도구 | 프로젝트 루트 아래 대상 | 명시적 호출 예시 |
|---|---|---|
| Codex | `.agents/skills/kdev-start/SKILL.md` 등 | `$kdev-start`를 사용해 프로젝트 계약을 작성해 주세요. |
| Claude Code | `.claude/skills/kdev-start/SKILL.md` 등 | `/kdev-start`를 사용해 프로젝트 계약을 작성해 주세요. |

`skills/` 안의 **각 스킬 폴더**를 대상 skills 폴더 바로 아래로 복사합니다. `skills/skills/`로 중첩하지 않습니다. 두 도구 모두 쓰는 경우 필요한 대상에 각각 설치하되, 이미 호환 경로에서 읽히는 동일 스킬은 중복 설치하지 않습니다.

출처: [Codex Skills](https://learn.chatgpt.com/docs/build-skills), [Claude Code Skills](https://code.claude.com/docs/en/skills). 문서상 경로와 로컬 패키지 검증을 확인했으며 두 제품의 모든 버전에서 실제 호출한 결과는 아닙니다.

## 에이전트가 수행할 복사 절차

- 모든 대상 폴더의 충돌을 **복사 전에** 확인합니다. 파일·폴더·심볼릭 링크를 포함해 같은 이름이 하나라도 있으면 전체 복사를 멈추고 위치와 차이를 보고합니다. 덮어쓰기·병합·삭제는 하지 않습니다.
- 대상 프로젝트 밖을 가리키는 심볼릭 링크가 있는지 확인합니다. 해당 경로를 따라 전역 폴더에 쓰지 않습니다.
- 대상 skills 디렉터리가 없으면 만듭니다. 다른 스킬과 `AGENTS.md`, `CLAUDE.md`, 설정·키 파일은 수정하지 않습니다.
- 선택한 8개 스킬 폴더를 그대로 복사하고 원본과 파일 내용이 같은지 확인합니다. 중간 오류는 성공으로 보고하지 말고 복사된 목록과 남은 작업을 기록합니다.
- 원본 URL 또는 로컬 ZIP 경로, 태그/커밋(없으면 미확정), 버전, 설치 경로·파일 목록을 프로젝트의 `kdev-installation.md`에 기록합니다. 해당 기록 파일도 기존 것이 있으면 덮어쓰지 말고 사용자에게 차이를 보여줍니다.

현재 패키지는 Markdown 지침만 포함하며 실행 스크립트·네트워크 연결·외부 계정이 필요하지 않습니다. 파일 복사를 위해 운영체제의 일반 파일 기능을 사용하면 됩니다. GitHub 다운로드에는 해당 네트워크 접근이 필요합니다.

## 발견·호출 확인

프로젝트 루트에서 새 세션을 열고 위 표의 명시적 호출을 사용합니다. 파일이 보이지 않으면 도구 재시작/새 세션 후 위치·메타데이터를 확인합니다. 현재 세션이 다른 곳에서 시작되었거나 목록이 갱신되지 않았을 수 있습니다.

첫 요청:

> 프로젝트 시작 스킬을 사용해 저 혼자 로컬에서 쓸 독서 기록 도구의 짧은 계약을 작성해 주세요. 기록·검색·수정이 목적이고 로그인과 클라우드는 제외합니다. 이번 요청은 문서 작성까지만 진행해 주세요.

관찰할 결과: 개인용 범위·시나리오·입출력·저장·대표 실패·완료 조건을 작성하고, 기업 인프라나 구현을 임의 추가하지 않습니다. “스킬을 사용했습니다”라는 말만으로 성공 판정하지 말고 읽은 파일·실제 산출물을 확인합니다. 호출할 수 없으면 직접 문서를 읽게 하는 수동 사용과 자동 발견 성공을 구분합니다.

## 업데이트와 제거

업데이트는 기존 설치와 새 버전의 차이를 먼저 검토합니다. 학생이 수정한 파일을 보존하고, 승인한 변경만 적용합니다. 첫 버전은 자동 업데이트를 제공하지 않습니다.

제거할 때는 설치 기록을 기준으로 본 패키지 폴더만 확인합니다. 현재 내용이 설치 당시와 달라졌다면 먼저 보존 위치를 결정합니다. 다른 스킬·설정이나 상위 skills 디렉터리 전체를 삭제하지 않습니다.
